import { View } from "react-native";

const ProfileScreen = () => {

    // komponenter:
    // profilbild
    // namn (text)
    // input favoritböcker (med 2 keyboards)

    return (
        <View></View>
    )   
}

export default ProfileScreen